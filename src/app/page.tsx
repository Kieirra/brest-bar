import BarList from "@/components/features/explorer/organisms/bar-list";

const Home = async () => {
    return (
        <main>
            <BarList />
        </main>
    );
};

export default Home;
